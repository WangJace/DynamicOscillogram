# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DynamicOscillogram' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicOscillogram
  pod "CorePlot", '~> 2.2'

  target 'DynamicOscillogramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicOscillogramUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
